# AGENTS.md

## Cursor Cloud specific instructions

This repository ("ClaudeCodeMobileIDE") is currently an empty/initialized project containing only a `README.md`. There are no source files, dependencies, build systems, or services defined yet.

- **No package manager or dependency file** exists (`package.json`, `requirements.txt`, `Cargo.toml`, etc.).
- **No build/test/lint tooling** is configured.
- **No Docker, devcontainer, or CI/CD configuration** is present.
- **No services** need to be started.

Once the project gains code and dependencies, this section should be updated with setup caveats, service startup instructions, and any non-obvious development notes.
